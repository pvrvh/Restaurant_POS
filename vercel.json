{
  "buildCommand": "cd pos-frontend && npm install && npm run build",
  "outputDirectory": "pos-frontend/dist",
  "installCommand": "npm install --prefix pos-frontend && npm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}  